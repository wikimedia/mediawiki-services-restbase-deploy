#!/bin/bash


cd $(dirname ${0})/..;
rm -rf node_modules;

npm install;

cd node_modules;
rm -rf preq restbase-mod-table-spec;

ln -s /home/doorman/code/wikimiki/preq preq;
ln -s /home/doorman/code/wikimiki/restbase-spec restbase-mod-table-spec;

exit 0;

