/**
 * Request templating
 */

var exampleTemplate = {
    uri: '/template/{with}/{parameters}',
    headers: {
        'content-type': '$req.headers.content-type',
    },
    body: '$req.body'
};

// - problem: need checks for nested lookups (query, headers, body)
//     - failure handling: fail if not set?
//
